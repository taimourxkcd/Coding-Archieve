#include<stdio.h>
#include<conio.h>
float dollar_rate(float);
int main()
{
float result,y;
 printf("The rate of the dollar is:\n");
 result=dollar_rate(y);
 printf("the rate of the dollar is %.1f",result);
 getchar();
 return 0;
 }

 float dollar_rate(float x)
 {
  x=85.6;
  return x;
 }
