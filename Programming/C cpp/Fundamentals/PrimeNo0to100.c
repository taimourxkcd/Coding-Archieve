#include<stdio.h>
#include<conio.h>
void main()
{
int num;
for(num=0;num<=100 && num/num==1 && num/1==num;num++){
printf("following is the list of all the prime numbers between 0 and 100\n");
printf("%d\n",num);}

getch();
}
